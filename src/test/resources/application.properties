quarkus.http.test-port=0

# HMAC secret injected via property/env var to avoid committing key files
jwt.hmac.secret=${JWT_HMAC_SECRET}
smallrye.jwt.sign.secretkey=${jwt.hmac.secret}
smallrye.jwt.verify.secretkey=${jwt.hmac.secret}
smallrye.jwt.verify.algorithm=HS256